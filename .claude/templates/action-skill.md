---
kind: action-skill
id: __ID__
version: 1
title: __TITLE__
description: __DESCRIPTION__
inputs: [pr-diff, file-path]
outputs: [findings-report]
bc-version: [all]
technologies: [al]
countries: [w1]
application-area: [all]
---

# __TITLE__

__INTRO__ This is a leaf action skill: it invokes no sub-skills.

## Source

Read the BCQuality knowledge index once (`knowledge-index.json` at the checkout root, regenerated by Entry's preparation step). Take the index entries whose `domain` is `__DOMAIN__` as the candidate set across every enabled layer. Do not open article files at this step.

## Relevance

Apply the frontmatter matching rules defined in READ (*Frontmatter matching semantics*) against the task context: `bc-version` from the app's `app.json` or the orchestrator; `technologies` = `[al]`; `countries` and `application-area` from the consuming app, or `unknown`. Discard non-applicable files. Findings from conditionally applicable files carry `confidence` no higher than `medium` and name the unknown dimension in `message`.

## Worklist

__WORKLIST__

Read an article's full body only after it makes the worklist. Resolve layer-precedence conflicts per READ and record suppressions. When no signal matches, return `outcome: "not-applicable"` without evaluating files.

## Action

For each worklist entry, evaluate the input against the article's `## Best Practice` and `## Anti Pattern` sections. Emit `major` (or `blocker` where the article's consequence is irreversible) on a clear Anti Pattern match, `minor` on a Best Practice contradiction, and nothing when no violation is present. Apply the reference-integrity gate and the agent-finding precision bar from `skills/do.md`. Emit `suggested-code` for mechanical fixes, adapting the `.good.al` companion when the diff matches the `.bad.al` shape.

## Output

Output conforms to the DO output contract. Every finding this skill emits MUST set `findings[].domain` to `"__DOMAIN_LABEL__"`.
